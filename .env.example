# Vite admin — copy thành .env và chỉnh giá trị
VITE_BASE_URL=http://localhost:3000/api/

# Khớp TRUST_BLACKLIST_THRESHOLD trên backend (mặc định server: 50).
# Nếu không đặt, UI dùng 50 — cùng mặc định server.
# VITE_TRUST_BLACKLIST_THRESHOLD=50
